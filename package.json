{
  "name": "music-wise",
  "private": true,
  "version": "0.0.1",
  "description": "music-wise",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@ionic/core": "^5.6.3",
    "@stencil/core": "^2.5.1"
  }
}
